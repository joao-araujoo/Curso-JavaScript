let vetor = [ , , , ]
for(let x = 0; x <= 3; x++){
    vetor[x] = prompt(`Digite o ${x + 1}° coiso: `)
} 

for(let x = 0; x <= 3; x++){
    alert(`Como ${x + 1}° termo, você digitou: ${vetor[x]}`)
} 